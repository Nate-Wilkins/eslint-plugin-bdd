/**
 * @global iit
 * @global ddescribe
 * @global describe
 * @global it
 * @global expect
 *
 * @description:
 * Tests that all tests are implemented
 */
'use strict';

(function (root) {
	describe('something', function () {
		it('should do stuff', function () {

		});
	});
}(this));
