/**
 * @global iit
 * @global ddescribe
 * @global describe
 * @global it
 * @global expect
 *
 * @description:
 * Tests that there are no focus tests
 */
'use strict';

(function (root) {
	describe('something', function () {
		it('should do stuff', function () {

		});
	});
}(this));
